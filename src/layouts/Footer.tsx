// import { Box } from "@mui/material";
import Logo from "../components/Logo";

const Footer = () => {
  return (
    <footer className="min-h-[100px] bg-slate-800 flex justify-center items-center">
      <Logo />
    </footer>
  );
};

export default Footer;
